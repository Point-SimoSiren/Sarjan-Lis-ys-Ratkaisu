@model IEnumerable<MVCjsonTesti.Models.Treenihistoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pvm)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TreenihistoriaID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pvm)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TreenihistoriaID)
        </td>
        <td>

            <!-- LINKKI JOSTA SARJAN LUOMINEN KÄYNNISTYY. -->
            @Html.ActionLink("Lisää sarja", "Sarja", new { id = item.TreenihistoriaID }) |


            @Html.ActionLink("Edit", "Edit", new { id = item.TreenihistoriaID }) |
            @Html.ActionLink("Details", "Details", new { id = item.TreenihistoriaID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.TreenihistoriaID })
        </td>
    </tr>
}

</table>
